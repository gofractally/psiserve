# psio tools — fixture emitters (emit_xval_*.cpp) and psio_tool
# CLI. Phase 0 scaffold: empty subdir. Emitters land in Phase 6 and
# beyond as format support arrives; psio_tool lands with Phase 14's
# schema + transcode work.

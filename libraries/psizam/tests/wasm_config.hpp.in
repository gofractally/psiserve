#pragma once

namespace psizam {
   static constexpr auto host_wasm = "${CMAKE_CURRENT_SOURCE_DIR}/host.wasm";
   static constexpr auto wasm_directory = "${CMAKE_BINARY_DIR}/wasms/";
} // namespace psizam

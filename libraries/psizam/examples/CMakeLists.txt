# psizam examples — opt-in walkthroughs of the authoring pipeline.
# Each subdir is a self-contained target; consumers can copy any of
# them out as a starting point.

add_subdirectory(hello_world)
add_subdirectory(composition)
add_subdirectory(runtime)
add_subdirectory(runtime_resource)
